import React from 'react';
import { View, Text } from 'react-native';
import { Tabs } from './config/router';

const App = () => (
  <View style={{ flex: 1 }}>
    <Tabs />
  </View>
);

export default Tabs;
